---
title: 微积分笔记：导数、微分与中值定理
date: '2018-07-31 12:00:29'
updated: '2026-09-24 11:00:00'
abbrlink: e76b2003
categories:
- 软件
- 数学基础
tags:
- 数学
- 微积分
series: 数学基础笔记
description: 从平均速度与瞬时速度理解导数，区分微分、罗尔定理和拉格朗日中值定理，用小例子说明导数与单调性的关系。
source_platform: 简书
source_url: https://www.jianshu.com/p/eeafa2453260
source_title: 微积分笔记   中值定理（MVT）
toc: true
source_urls:
- https://www.jianshu.com/p/eeafa2453260
- https://www.jianshu.com/p/df0a33092a72
source_titles:
- 微积分笔记   中值定理（MVT）
- 微积分（一） 微分
katex: true
---

“对于驾车而言，某一时刻的速度必然等于平均速度。”这是原来记中值定理时最直观的一句话。

这篇合并了 2018 年的《微分》和《中值定理》两份笔记。《微分》当时只有两张图片，如今连同中值定理的四张图都无法恢复。因此，下面的导数定义和计算例子是本次整理补充的文字，不是对原图的复原。

[数学基础笔记目录](/series/mathematics/)

## 从平均变化率到导数

在区间 `[a, b]` 上，函数的平均变化率为：

{% math %}
\begin{gathered}
\frac{f(b)-f(a)}{b-a}\\
a<b
\end{gathered}
{% endmath %}

如果 f 表示位置、横轴表示时间，这就是这段时间内的平均速度。把区间不断缩短，才会走到某一时刻的瞬时变化率：

{% math %}
f'(x)=\lim_{h\to0}\frac{f(x+h)-f(x)}h
{% endmath %}

这个极限存在时，函数在该点可导。例如 `f(x) = x²`：

{% math %}
\frac{(x+h)^2-x^2}h=2x+h\quad\Longrightarrow\quad f'(x)=2x
{% endmath %}

## 微分与实际增量

实际增量是 `Δy = f(x + Δx) − f(x)`。函数在 x 处可导时，微分给出它的一阶线性近似：

{% math %}
\begin{gathered}
\mathrm dy=f'(x)\,\mathrm dx\\
\mathrm dx=\Delta x
\end{gathered}
{% endmath %}

仍然看 `f(x) = x²`，在 `x = 3`、`Δx = 0.01` 时，实际增量是 `0.0601`，微分是 `6 × 0.01 = 0.06`。两者很接近，但不是完全相等。

## 拉格朗日中值定理

设函数 f 在闭区间 `[a, b]` 上连续，在开区间 `(a, b)` 内可导。那么至少存在一个 `c ∈ (a, b)`，使得：

{% math %}
f'(c)=\frac{f(b)-f(a)}{b-a}
{% endmath %}

这正是开头那句话对应的数学形式。以 `f(x) = x²` 在 `[1, 3]` 上为例，平均变化率是 4，而 `f'(2) = 4`。

连续性和可导性的条件不能省略。例如 `f(x) = |x|` 在 `[-1, 1]` 上连续，却在 0 处不可导。它的平均变化率是 0，但区间内有导数的点，其导数只有 -1 或 1。

## 罗尔定理是其中的特殊情况

原笔记把 MVT 直接称为罗尔定理，这里需要纠正。若在前面的条件之外，还有 `f(a) = f(b)`，那么中值定理右侧为 0：

{% math %}
f'(c)=0
{% endmath %}

这就是罗尔定理的结论。以 `f(x) = x²` 在 `[-1, 1]` 上为例，两端函数值相同，中间 `c = 0` 处的导数为 0。

## 导数与单调性

原稿的推论漏掉了导数符号，写成“f(x) 大于零则递增”。函数值为正，只能说明图像在横轴上方，不能决定它是否递增。

在相应区间满足连续、可导条件时：

- 区间内部处处 `f'(x) > 0`，函数严格递增。
- 区间内部处处 `f'(x) < 0`，函数严格递减。
- 区间内部处处 `f'(x) = 0`，函数为常函数。

“某一个点的导数为零”不等于函数在整段区间恒定。`x²` 在 0 处导数为零，但它显然不是常函数。

## 对照阅读

[OpenStax Calculus Volume 1：The Mean Value Theorem](https://openstax.org/books/calculus-volume-1/pages/4-4-the-mean-value-theorem)
